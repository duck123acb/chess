use crate::utils::PieceType;

struct Board {
  bitboards: [u64; 12],
  // add other flags when needed
}
impl Board {
  fn parse_fen(&mut self) {

  }
  fn export() { // return board as an array for the renderer to draw
    
  }
}